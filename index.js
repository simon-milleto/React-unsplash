import { AppRegistry } from 'react-native';
import AppProvider from './src/AppProvider';

console.disableYellowBox = true;

AppRegistry.registerComponent('test', () => AppProvider);


 // npm i --save react-navigation redux react-redux
